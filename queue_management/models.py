"""
Queue Management Models
Note: Queue models are in appointments.models (AppointmentQueue) to maintain
database relationships. This app provides service layer and views.
"""
# Models are in appointments.models to avoid circular dependencies
# and maintain proper foreign key relationships
